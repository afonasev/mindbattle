## 1. Каноническое правило и taxonomy

- [x] 1.1 Дополнить `docs/GAME_SPEC.md` и журнал изменений утверждённым правилом разнообразия областей тем, сохранив существующие границы classic-v1; проверить `git diff --check`.
- [x] 1.2 Ввести конечную область для каждой из 110 записей taxonomy-manifest и валидировать полноту/допустимость; проверить `npm run content-check`.

## 2. Детерминированный выбор тем

- [x] 2.1 Реализовать выбор набора с максимальным числом разных областей до tie-break, не меняя сериализуемое состояние матча; проверить typecheck.
- [x] 2.2 Добавить unit-тесты обычного, бонусного и финального списков: отсутствие двух исторических тем при доступных альтернативных областях, fallback при нехватке областей, уникальность ID, исключения и одинаковый seed/state; проверить целевые тесты Vitest.

## 3. Единый выбор режима

- [x] 3.1 Сделать «На одном устройстве (2–4)» вторичным действием без изменения обработчика, семантики, keyboard/focus-поведения и mobile-visibility; проверить browser-тест меню с assertion визуального варианта и открытия classic setup.
- [x] 3.2 Выполнить muted browser QA главного меню в целевом desktop viewport, сохранить и визуально проверить PNG с выровненными действиями режима.

## 4. Сквозная проверка

- [x] 4.1 Выполнить `npm run check`, `openspec validate diverse-topic-selection --strict` и сохранить точные результаты в отчёте.
- [x] 4.2 Физический local multiplayer playtest не повторяется: пользователь подтвердил, что он нужен только при изменении device-specific логики или взаимодействия, которых этот change не затрагивает; выбор тем и меню покрыты unit-, browser- и build-проверками.
