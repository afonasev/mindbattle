## 1. Авторитетное состояние и ввод

- [x] 1.1 Заменить командные `responses` и этапы оценки сложности на один сериализуемый общий complaint-result с выбором «Нет» по умолчанию, причинами, заметкой как самостоятельным сигналом, курсором и устойчивым v3 eventId; проверить unit-тестами «Нет», валидную жалобу, пустую жалобу, взаимоисключение сложности, continuation и отсутствие влияния на правила партии.
- [x] 1.2 Обновить миграцию и валидацию snapshot schema до v3: v3 ожидающий результат восстанавливает тот же eventId, а старый незавершённый v1/v2 фидбэк безопасно продолжает игру без новой записи; проверить сериализационными unit-тестами.
- [x] 1.3 Перевести keyboard/gamepad adapter и reducer на общий фидбэк без teamId, с rising-edge/neutral gate, корректной вертикальной навигацией трёхколоночной сетки тегов, confirm-клавишами для продолжения и детерминированной первой валидной командой в одном input frame; проверить unit-тестами для двух назначенных источников, удержания и фокуса заметки.

## 2. Локальная запись и редакторский сигнал

- [x] 2.1 Добавить v3 event contract и server validation для `hasComplaint`, причин и заметки до 500 Unicode-кодпоинтов, включая заметку как самостоятельную жалобу, сохранив разбор v1/v2; проверить unit-тестами положительный сигнал, жалобу, конфликт eventId, невалидные сочетания и границу заметки.
- [x] 2.2 Обновить append-only NDJSON store и GameController: для «Нет» и «Да» отправляется ровно одно идемпотентное событие, при ошибке общий результат повторяется с тем же eventId; проверить unit-тестами и API-тестами, включая восстановление после перезапуска.
- [x] 2.3 Заменить content-audit сводку perceived difficulty на отдельные v3 показы, жалобы, отсутствие жалоб и причины; оставить v1/v2 отдельными и исключить текст заметок из агрегатов; проверить unit-тестами смешанный журнал и ручную доступность v3-записи.

## 3. Общий интерфейс и документация

- [x] 3.1 Заменить экран независимых анкет на общий экран «Хотите пожаловаться на вопрос?» с выделенным «Нет», немедленным continuation без повторного списка вариантов, выбором нескольких причин для «Да», заметкой как самостоятельным сигналом, явным взаимоисключением сложности и retry только при ошибке записи; проверить typecheck и DOM-рендером без командных маркеров/статусов.
- [x] 3.2 Обновить `docs/GAME_SPEC.md` и журнал изменений утверждённым complaint-flow, v3 локальным журналом, заметкой как самостоятельным сигналом, confirm-клавишами и отсутствием командной принадлежности; сверить каждое изменённое правило с delta-specs.

## 4. Проверка и доставка

- [x] 4.1 Выполнить `npm run check` и релевантные unit/API-тесты; зафиксировать краткий результат, включая v1/v2 совместимость.
- [x] 4.2 Выполнить `openspec validate streamline-question-feedback --strict` после синхронизации артефактов и исправить все замечания.
- [x] 4.3 Провести muted Playwright-плейтест в актуальном in-app Browser: «Нет» сохраняется и продолжает игру, «Да» требует причину, заметка вводится без игровых действий, ошибка записи повторяется с тем же eventId; приложить актуальные скриншоты общего экрана.
