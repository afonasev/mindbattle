## 1. Контракт и ввод

- [x] 1.1 Зафиксировать горизонтальную схему bonus-veto в актуальном `docs/GAME_SPEC.md` и журнале изменений; проверить отсутствие противоречащего актуального правила.
- [x] 1.2 Переназначить keyboard и gamepad directions в адаптере на left/right cursor, down confirm, up cancel; подтвердить mapping, rising edge и neutral gate unit-тестами.
- [x] 1.3 Обновить gamepad/keyboard control glyphs и профили так, чтобы подсказки каждой схемы совпадали с фактическими действиями; проверить Xbox/PlayStation/Nintendo/universal labels тестами без заявления физического playtest.

## 2. Интерфейс и целевая проверка

- [x] 2.1 Заменить текст подсказки бонусного экрана на `←/→ курсор · ↓ запретить или заменить · ↑ снять запрет` с перечислением D-pad/WASD/стрелок; проверить читаемость и отсутствие overflow на 1280×720 и 1920×1080.
- [x] 2.2 Обновить Playwright-сценарии для 2–4 команд: фактически переместить каждый публичный курсор по горизонтали, установить разные запреты вертикальным действием и перейти к вопросу; сохранить актуальный screenshot бонусного экрана.

## 3. Завершение change

- [x] 3.1 Выполнить `npm run check`, Chromium playtest, `git diff --check` и `openspec validate align-bonus-veto-navigation --strict`; зафиксировать аппаратную оговорку для физического gamepad.
- [x] 3.2 Провести отдельный `$openspec-verify-change`, синхронизировать delta с основной `controller-input` spec и подготовить точный staged scope без пользовательских `AGENTS.md` и `.codex/` к отдельному архивированию и атомарному коммиту.
