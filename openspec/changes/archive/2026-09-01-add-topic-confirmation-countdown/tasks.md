## 1. Канонический контракт и доменное состояние

- [x] 1.1 Синхронизировать `docs/GAME_SPEC.md` и журнал изменений: только после обычного выбора темы показать трёхсекундное подтверждение с новым вводом для пропуска; проверить отсутствие правила о перебивках bonus-veto или tie-break.
- [x] 1.2 Добавить сериализуемую фазу подтверждения темы, её монотонный отсчёт, pause/resume и ручной `continue`; проверить unit-тестами, что вопрос и базовый таймер начинаются только после auto/manual transition, а одна и та же input frame не создаёт ответ.
- [x] 1.3 Расширить строгую сериализацию и восстановление snapshot новой фазой; проверить round-trip, повреждённый остаток и восстановление с паузой без утечки трёх секунд.

## 2. Ввод и представление

- [x] 2.1 Подключить preparation phase к input mode и neutral/rising-edge gate для keyboard и standard Gamepad API; проверить unit-тестами, что удержание выбора темы не пропускает экран, а новое action вызывает ровно один `continue`.
- [x] 2.2 Добавить отдельный DOM/CSS-экран названия темы, трёхсекундного счётчика и подсказки раннего перехода; проверить, что вопрос и варианты до старта не рендерятся, а reduced motion не меняет длительность или доступность пропуска.
- [x] 2.3 Запустить Chromium-сценарий с 2–4 командами: выбрать обычную тему, проверить auto start и manual skip после отпускания, отсутствие преждевременного answer timer и pause/resume; сохранить screenshots нового экрана на 1280×720 и 1920×1080.

## 3. Завершение change

- [x] 3.1 Выполнить `npm run check`, `git diff --check`, `openspec validate add-topic-confirmation-countdown --strict` и реальный локальный multiplayer playtest с назначенными keyboard-схемами; отдельно зафиксировать, выполнен ли физический gamepad-плейтест. Выполнено: пользователь подтвердил локальный групповой keyboard-плейтест; физические gamepad не проверялись.
- [x] 3.2 Провести отдельный `$openspec-verify-change`, синхронизировать принятые delta specs с каноническими specs, архивировать только завершённый change и создать один атомарный Git-коммит; изменения `starterOnly` в общих доменных файлах включены по явному разрешению пользователя.
