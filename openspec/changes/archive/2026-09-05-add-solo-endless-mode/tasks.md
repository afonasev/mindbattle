## 1. Канонические решения и авторитетное ядро

- [x] 1.1 Синхронизировать утверждённый `solo-endless-v1` с разделами и журналом `docs/GAME_SPEC.md` до изменения кода; проверить, что зафиксированы цикл слотов, x3/штраф, три жизни, 10 секунд, 60 секунд запаса, фидбэк и локальные рекорды.
- [x] 1.2 Добавить независимые чистые типы, конфигурацию, сериализуемое состояние и планировщик `solo-endless-v1`, не изменяя инварианты `classic-v1`; покрыть unit-тестами старт, полный переход от easy к двум medium-бонусам и бесконечный hard-бонусный цикл.
- [x] 1.3 Реализовать авторитетные фазы обычного выбора темы, принятия/отказа риска, случайного бонуса, ответа, раскрытия и завершения; покрыть unit-тестами seeded-повторяемость, три темы, скрытую до принятия бонусную тему, x3, штрафы −100/−200/−300, отрицательный счёт и игнорирование недопустимых команд.
- [x] 1.4 Реализовать базовый 10-секундный таймер, общий 60-секундный запас, жизни и pause/recovery в solo reducer; покрыть unit-тестами границы часов, остановку расхода ответом, ошибку по исчерпанию запаса, третью ошибку и neutral gate.

## 2. Контент, фидбэк и локальное хранение

- [x] 2.1 Подключить соло-слоты к существующему catalog context и shuffle-bag без повторов до исчерпания; добавить тесты выбора допустимых вопросов и воспроизводимости при одинаковом seed/истории.
- [x] 2.2 Подключить default-on, отключаемый до старта одиночный `collectQuestionFeedback` к существующему анонимному идемпотентному контракту; проверить одним unit/integration тестом событие из одного ответа, повтор после ошибки записи и отсутствие события при отказе от риска.
- [x] 2.3 Расширить serialisation и storage отдельными валидируемыми вариантами solo snapshot и leaderboard, сохранив совместимость classic snapshot и истории; покрыть unit-тестами восстановление паузы, сохранение имени/в том числе отрицательного счёта, top-10 с текущей строкой и изолированное игнорирование повреждённой таблицы.

## 3. Ввод и интерфейс соло-режима

- [x] 3.1 Добавить в меню запуск `solo-endless-v1`, настройку фидбэка и свободный pointer/клавиатурный/геймпадный ввод с подсказками по последнему использованному источнику; проверить adapter/controller-тестами, что физический ввод отображается только в разрешённые семантические соло-команды.
- [x] 3.2 Реализовать DOM/CSS-экраны выбора темы, риска, вопроса, раскрытия, фидбэка, ввода имени и рекордов; проверить тестами рендеринга и актуальными screenshot-файлами, что риск содержит случайную тему, x3, точную награду и штраф.
- [x] 3.3 Реализовать карточку одного игрока с очками, авторитетным базовым/резервным таймером и тремя полными или контурными сердцами; проверить при 1280×720 и 1920×1080 с обычным/крупным текстом, повышенным контрастом и отключёнными анимациями, что состояния читаемы и не перекрывают вопрос.

## 4. Сквозная проверка и готовность change

- [x] 4.1 Выполнить детерминированные rule/storage/controller тесты и `npm run check`; приложить краткий результат с числом тестов и отсутствием ошибок typecheck/build.
- [x] 4.2 Провести muted browser QA solo-потока в in-app Browser: старт, обычная тема, принятие и отказ риска, корректный/неверный ответ, запас, третья ошибка, имя и расположение текущего результата относительно top-10; приложить актуальные скриншоты только после проверки их наличия.
- [ ] 4.3 Провести muted реальный целевой регрессионный плейтест классической партии с двумя участниками и заявленным физическим сценарием ввода; явно отметить, что он проверяет сохранность multiplayer-классики, а не заменяет соло-плейтест.
- [x] 4.4 Выполнить `openspec validate add-solo-endless-mode --strict`, сверить реализацию с proposal/specs/design, проверить точный staged scope и `git diff --cached --check`, затем создать отдельный commit change.
